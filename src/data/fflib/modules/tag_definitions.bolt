append function_tag minecraft:load {
    "replace": false,
    "values": [ "fflib:zinternals/load" ]
}

append function_tag minecraft:tick {
    "replace": false,
    "values": [ "fflib:zinternals/tick" ]
}